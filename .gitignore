# secrets — never commit
.env
*.key
secret.bat
# local recipient list; use the RECIPIENTS secret in CI
recipients.yaml

# local output from the POC
out.json
results.json

# python
__pycache__/
*.pyc
.venv/
venv/
