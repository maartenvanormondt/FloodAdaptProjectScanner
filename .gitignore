# secrets — never commit
.env
*.key
secret.bat
# local recipient list; use the RECIPIENTS secret in CI
recipients.yaml

# local output from the POC
out.json
results.json
data/new.json
# transient per-run scout output (merged into the DB; not worth tracking)
data/opportunities.json

# python
__pycache__/
*.pyc
.venv/
venv/
